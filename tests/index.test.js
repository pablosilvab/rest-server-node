'use strict'

describe('sum two values', () => {
    test('sum two values', () => {
        const a =1
        const b = 2
        const expected = 3
        const result = a+b
        expect(result).toBe(expected)
    });
});